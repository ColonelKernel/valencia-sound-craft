# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.lovable/logs/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

# Ralph gate evidence (local only)
specs/*/gate-logs/

# Playwright artifacts
test-results/
playwright-report/

# Claude Code transient artifacts (must never be committed): git worktrees
# create orphaned gitlinks that break `git clone --recurse-submodules` on
# static hosts (Netlify), and launch.json is local dev-server config.
.claude/worktrees/
.claude/launch.json
