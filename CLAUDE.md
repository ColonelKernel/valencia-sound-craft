# Claude — start here

**Read the constitution:** [`.specify/memory/constitution.md`](.specify/memory/constitution.md)

That file is the single source of truth for this project. It covers:

- What this repo is (public PhD-application research dossier).
- The three core content principles: curate not catalog, verify metrics,
  observability is part of the result.
- The test-enforced publishing safeguards (`src/content/research.test.ts`).
- Autonomy configuration (YOLO Mode, Git Autonomy, `main`-branch guard).
- The Ralph loop workflow: spec → branch → run → PR.
- The full quality-gate command list.

Read it on every session, whether you are in a Ralph loop or an interactive
chat. When they disagree, the tests win and the constitution is out of date.
